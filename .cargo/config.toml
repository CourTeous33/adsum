[build]
# v0 mangling scheme provides more detailed backtraces around closures
rustflags = ["-C", "symbol-mangling-version=v0"]

[env]
# Required to match the macOS deployment target Zed compiles its Metal
# shaders against; mismatching this can produce subtle runtime issues.
MACOSX_DEPLOYMENT_TARGET = "10.15.7"
